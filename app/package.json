{
  "name": "ci-baseline-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Node.js demo used as the CI baseline target for Week 1 Day 05.",
  "main": "src/index.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "eslint src test",
    "test": "node --test test/*.test.js",
    "build": "node scripts/build.js",
    "start": "node src/index.js"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "@eslint/js": "^9.0.0"
  }
}
